apiVersion: v1
kind: Service
metadata:
  name: container-web
  labels:
    app: container-web
spec:
  type: NodePort

  selector:
    app: container-web

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
